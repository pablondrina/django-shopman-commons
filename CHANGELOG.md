# Changelog

## 0.1.0 (2026-02-23)

Initial release.

- `monetary_mult` / `monetary_div` — canonical monetary rounding (ROUND_HALF_UP)
- `format_money` — centavos → formatted string with comma decimal separator
- `monetary_div` divisor validation (must be > 0)
